Automatic dynlib testing
------------------------

Ideally all diagnostics should be tested.
 -> Required input data for diagnostics:
    isentropic: u, v, mont [, z] <, !lat, !fcor> 
    pressure/any: u, v, w, z, q, T [, mv, rh, e] <, p, rho, uz, vz, !lat, !beta, lon>
     
 -> Required input data for ellipse??

Composites based on the test data.

EOFs based on the test data.
 * Incl. varimax rotated EOFs.

Reading and writing should be tested for all defined contexts.

Standard plotting functions should not throw errors
 * map + overlays
 * section + overlays

Python settings infrastructure
 * Set overrides and remove them
 * Set defaults and remove them
 * Set overrides and defaults and remove them (separately)
 * Context handling: define/check-if-in

Python convenience functions should be tested
 * dts2str
 * aggregate and time iterators in tagg
 * cal_mfv
 * line utils: Mask, mask-with-data, Smear


User-assisted dynlib testing
----------------------------

Visually check result of every plotconf argument

